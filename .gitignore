# Python
__pycache__/
*.py[cod]
*.so
*.egg-info/
*.egg

# Virtual environments
venv/
.venv/

# Test / coverage
.pytest_cache/
.coverage
htmlcov/

# Type checker / linter caches
.mypy_cache/
.ruff_cache/

# Jupyter
.ipynb_checkpoints/

# IDE
.idea/
.vscode/

# Docker artifacts
model_output/
model.tar.gz
model.safetensors
config.json
generation_config.json
preprocessor_config.json
special_tokens_map.json
tokenizer_config.json
training_args.bin
training_info.json
vocab.txt
sentencepiece.bpe.model

# Training data (generated, not tracked)
data/synthetic/

# Sample images (may contain personal info — never track)
data/samples/*
!data/samples/.gitkeep

# Postal data (downloaded at runtime, not tracked)
data/postal/*
!data/postal/.gitkeep